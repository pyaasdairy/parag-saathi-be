{
 "fix": "All seven items are done, each committed on `integration/delivery` (repo `C:\\Users\\jaina\\Downloads\\pyaas-saathi`), nothing pushed, `analysis_options.yaml` left uncommitted throughout. Final state: HEAD d90af82, `flutter analyze` clean, `flutter test` 117 passed / 11 skipped (the 11 are the opt-in live e2e surfaces; baseline was 102 / 1).\n\n**1. Route Overview row — 0f7356c** `feat(rider): the route Overview row shows each pack with its size`\n`lib/screens/rider/home/route_screen.dart`: `PackLines(stops[i].items, dense: true)` between the address and the km/COD pills. Test in `test/pack_volume_test.dart` pumps `RouteScreen` with a `RiderStore` at 360 px, switches to Overview, asserts both pills and no overflow (the Sequence view's pre-existing Call/Navigate bar overflow under the test font is drained before the switch, documented). analyze: No issues. test: +103 ~1 All tests passed.\n\n**2. Latent ellipsis path — a7b9af8** `fix(rider,store): the size pill is drawn even when the name already states it`\nPicked the smallest diff: `packSizeOf` (`lib/models/pack_item.dart`) now returns the variant whenever non-empty; the models' `label` getters keep their dedup for one-string prints. False comment in `lib/widgets/pack_lines.dart` rewritten. Tests: a 58-char name embedding \"500ml\" at 360 and 320 px in Roboto (loaded from the SDK's `bin/cache/artifacts/material_fonts/`, verified it loads on this machine; falls back to the wider test font if absent) — the pill is inside the row, its text does not exceed max lines, the name itself is cut. analyze clean; test +105 ~1.\n\n**3. Inventory per variant — 3458ce9** `fix(store): Inventory keeps stock per pack size, with the size on every row`\n`StoreSku` gains `variant`, `id` = `name|variant`, `label`, `matchesName`, `matches(name, [variant])`; `catalogFromStock` one row per name+variant; `deriveStock` keys by row id (name+size match first, then first row of the name for a size-less line, unknown unchanged). Callers read: `matches` only in `canon`; `deriveStock` in the Inventory tab and low-stock alert; adjust flow keyed by `adjustKey`, untouched. Row extracted to `lib/screens/store/widgets/store_inventory_row.dart` with `PackSizePill`; banner and `raiseLowStock` summary use `sku.label`; Products tile counts names. New `test/store_inventory_test.dart` (7 tests: two variants → two rows with own counts, variant-less → one row, alias, fallback, hardcoded catalog unchanged, widget at 360 px). analyze clean; test +114 ~1.\n\n**4. Honest live e2e — 114a455** `test(e2e): one live test per surface, plus the route Overview and Inventory rows`\n`test/e2e/pack_volume_live_e2e_test.dart` restructured to 11 ordered `testWidgets` (fresh app per surface; network legs hand results through module-level slots; a surface leg fails naming the network leg that must pass). Route Overview row and Inventory rows (from `storeStockItems` + `deriveStock`, asserting reserved 2 / 1 per size) added; wire/model assertions kept; still `skip: !kE2E`. Verified against a dead port with the define on: each leg fails for its own stated reason. `tool/e2e/README.md` updated. No backend booted. analyze clean; test +114 ~11.\n\n**5. Geofence semantics — 792c7a3** `fix(rider): an absent geoExact keeps the phone-side fence of release 26.07.03`\n`release/26.07.03` has no `deliver_swipe_screen.dart`; its fence lived in `proof_sheet.dart` (`_geofenceOk`: no fix or no pin → false, else ≤ 300 m). `deliver_swipe_screen.dart` now: `_fenced => geoExact != false`; one `_tooFar` getter drives the card, error line and gate (absent + no pin = fail-closed as deployed; true + no pin = server measures). Tests: far-from-door blocked for `true` and absent, absent within fence finishes, `false` not blocked; docs in `delivery_api.dart` updated. analyze clean; test +115 ~11.\n\n**6. Two LOW copy items — 5d86bdc** `fix(rider): OTP_REQUIRED reopens the OTP box; RING_BELL always reads \"Ring the bell\"`\n`CollectCashSheet`: `400 OTP_REQUIRED` on the manual confirm sets `_otpSent`, clears the unavailable copy, shows `RiderNotice(tr('The OTP did reach the customer. Enter it below.'))` (hi.json key appended). `DeliveryPrefsView`: `RING_BELL` prints \"Ring the bell\" regardless of `ring_bell` (never twice, never alongside the warning — matches the backend's own handover line). Tests added in `test/cash_collect_test.dart` and `test/rider_delivery_ui_test.dart`. analyze clean; test +117 ~11.\n\n**7. Handoff + orphan — d90af82** `docs: Saathi handoff brought up to date; orphaned ComplianceStep removed`\n`HANDOFF-CODEV-2026-09-20.md`: test count 117/11, items 2–5 struck as done, C3 deploy note, pack-size surface table, new open item (run the live e2e once C3 lands). `ComplianceStep` removed from `lib/models/rider.dart` (grep: zero references). analyze clean; test +117 ~11.\n\n**Left undone / notes**\n- The live e2e was not executed against a booted backend (as instructed); it compiles and fails honestly per leg without one.\n- The `skipped` count moved from 1 to 11 because item 4 asked for one test per surface; all are the same opt-in file.\n- Item 2 trade-off: a name like \"Taaza 500ml\" now shows a \"500ml\" pill beside it (the option the task listed first, smallest diff); the `label` string getters still dedup.\n- Item 3: an order line whose size the wire omits, on a name split into variants, counts on the first row of that name (documented in `deriveStock`); `openingByName` remains name-keyed (unused in `lib/`).",
 "reviews": [
  {
   "verdict": "GREEN",
   "tests": "Repo C:\\Users\\jaina\\Downloads\\pyaas-saathi at HEAD d90af82, nothing edited or committed. Throwaway file test/zz_review_pack_size_throwaway_test.dart (24 tests, deleted afterwards): loaded Roboto regular/medium/bold from the SDK's bin/cache/artifacts/material_fonts (asserted it loaded), viewport 360x800 at DPR 1, theme = buildTheme() with fontFamily Roboto. For each of two names - 'Full Cream Milk - Parag Gold' and the 45-char 'Full Cream Milk - Parag Gold Pouch Pack 500ml' (length asserted 45) - pumped a crate of 500ml x2 + 1L x1 of that one name through: RiderOrderCard WITH the Call/Navigate/Open bar, RiderOfferCard, RouteScreen (Sequence cards, then the Overview spine), RiderSearchScreen results, DeliverSwipeScreen, rider OrderDetailScreen (fetched over a fake Dio HttpClientAdapter on ApiClient.instance.dio + FlutterSecureStorage.setMockInitialValues), rider InventoryScreen 'Verify inventory' (same fake wire, lines shaped exactly as rider_ops_route.go serves them), StoreOrderCard, StoreUpcomingCard, StoreOrderDetailScreen with the reduce steppers, and StoreInventoryRow built via catalogFromStock + deriveStock. Each pill surface asserted: '500ml' and '1L' found once each, exactly 2 PackSizePill, every pill rect within [0,360], pill RenderParagraph.didExceedMaxLines false, tester.takeException() null. Result: 24/24 passed. Then: flutter analyze = No issues found; flutter test on the tracked test files = 117 passed, 11 skipped (the opt-in live e2e), matching the implementer's report. A full 'flutter test' at one point counted 122 because another reviewer's untracked test/zz_reviewer_throwaway_test.dart was present; it vanished before the final run and git status ends at only ' M analysis_options.yaml'.",
   "findings": [
    "No pill surface hides a size. All nine rows that draw PackSizePill (RiderOrderCard, RiderOfferCard, Route Overview, DeliverSwipeScreen, rider OrderDetailScreen, StoreOrderCard, StoreUpcomingCard, StoreOrderDetailScreen, StoreInventoryRow) show both sizes of a same-name product at 360 px in Roboto, with the 45-char size-embedding name too: the name is the part cut (dense PackLines nameCut=true), the pill is whole and inside the viewport, no overflow. The Sequence view's RiderOrderCard with the Call/Navigate/Open bar also does not overflow under Roboto at 360 px, so the committed test's showActions:false and the drained exception in test/pack_volume_test.dart are artefacts of the wide test font, not of the row.",
    "One stock-counting row shows the size only as text, not as the series' pill: the rider's Verify-inventory sheet, lib/screens/rider/inventory/inventory_screen.dart (_lineCard, reached from lib/screens/rider/widgets/duty_strip.dart:236 and lib/screens/rider/profile/rider_profile_screen.dart:82). Its wire (InventoryLine.fromWire, lib/models/rider.dart:341-346) has no `variant`; the backend (parag-saathi-be internal/modules/consumer/rider_ops_route.go:649-669) keys lines by product_id and sends the pack size only as `unit` (catalogue unit '500 ml' / '1 L' from products_seed.json, else the order line's variant). The row prints it as 'Demand 2 500 ml' in bodySmall, which reads like 2500 ml, and with the 45-char name the name (maxLines 2) is cut on BOTH lines at 360 px Roboto (nameCut=[true,true]) because the stepper takes the width, leaving two identical truncated names told apart only by that meta line. The meta line itself is fully readable (asserted). This surface is unchanged from origin/release/26.07.03 and outside the seven items; recommend a follow-up that carries `variant` on this wire and draws PackSizePill on the line.",
    "Backend edge on the same sheet: rider_ops_route.go:641-643, when the order behind a task cannot be read, keys the demand by name with variant '' - two sizes of one product then MERGE into one line with no size at all. Rare fallback path, backend-side; noted for the backend agent.",
    "lib/screens/delivery/ (DeliveryCard prints only 'N items'; delivery_order_screen.dart:265 prints the joined DeliveryTaskItem.label; _OfferCard shows no items) is unrouted dead code (lib/screens/home_router.dart:58-61 routes RiderShell) - not a live surface, but it is the last lib/ consumer of the dedup `label` getter and would hide sizes if ever re-routed.",
    "Store home low-stock banner (lib/screens/store/store_home.dart:306) and StoreApi.raiseLowStock summary print sku.label ('name 500ml (3), name 1L (2)') in a Text with no maxLines - both sizes present; verified by reading only, since StoreHome needs AuthService/Provider and was not pumped.",
    "Frontend-to-backend alignment check (read-only, integration/delivery on both): task/order items carry productId|name|variant|qty (delivery.go:59-64) and upcoming items product_id|name|variant|qty (upcoming.go:32-37); stock lines carry name|category|variant|stock_count|in_stock (catalog.go:340-347); routes /stores/{id}/upcoming, /stores/{id}/stock (module.go:375,389) and /consumer/delivery/rider/inventory/today exist; geoExact is a non-omitempty bool (delivery.go:132) so the new backend always sends it while the phone's tri-state keeps the deployed fence when absent; item adjust reads product_id|variant|name (delivery_svc.go:528-536) which is what adjustItemsWire sends; OTP_REQUIRED is a 400 BadRequest while a live challenge exists (rider_ops_money.go:553); RING_BELL always yields 'Ring the bell and wait for the customer' (rider_ops_tasks.go:1035). Every wire read in the series matches; no wire key was renamed and DeliveryItem.name is still verbatim."
   ],
   "left_undone": [
    "Did not pump StoreHome itself (low-stock banner, Products tile) - it needs AuthService/Provider and several endpoints; those rows were verified by reading.",
    "Did not pump OrdersTab (reads RiderStore via Provider) - its rows are RiderOrderCard / RiderOfferCard, which were pumped directly.",
    "Did not run the opt-in live e2e against a booted backend (no backend was started, as instructed).",
    "Left another reviewer's untracked test/zz_reviewer_throwaway_test.dart alone when it appeared mid-run; it was gone by the final run."
   ]
  },
  {
   "verdict": "ISSUES",
   "tests": "Ran myself on C:\\Users\\jaina\\Downloads\\pyaas-saathi at d90af82: `flutter analyze` = \"No issues found!\"; `flutter test` = 117 passed, 11 skipped (the opt-in test/e2e file), exit 0 - matches the implementer report. Wrote one throwaway file test/zz_reviewer_throwaway_test.dart (5 tests, all passed) to prove the two geofence matrix cells the committed tests do not cover and two deriveStock edge behaviours, then deleted it; final `git status --short` is exactly ` M analysis_options.yaml`, as found. Backend and consumer repos were only read. NOTE: the backend working tree is mid-merge by the other agent (UU on delivery.go and delivery_svc.go, AA complaints.go), so every backend comparison below was taken from the committed `integration/delivery` tip via `git show integration/delivery:<path>`, and from `release/26.07.03` the same way.",
   "findings": [
    "COMPAT - no wire key renamed. Every read the series touches was compared with the committed backend struct on integration/delivery (delivery.go, models.go, catalog.go, rider_ops_money.go) and with release/26.07.03: task `geoExact` (backend `json:\"geoExact\"`, always emitted true/false on C3; absent on release) <-> `_tri(w['geoExact'] ?? w['geo_exact'])`; items[] `productId`/`name`/`variant`/`qty` (release had only name/qty -> variant '' -> no pill) <-> DeliveryTaskItem/DeliveryItem.fromWire; stock rows `name`/`category`/`variant`(omitempty)/`stock_count`/`in_stock` (identical struct on both branches) <-> storeStockItems; prefs `handover`/`ring_bell`/`call_before`/`note`/`receiver` <-> DeliveryPrefs.fromWire; error codes `OTP_REQUIRED` (400) and `OTP_UNAVAILABLE` (422) present at the same lines on both branches <-> `e.code` from the `{error:{code}}` envelope; POST /low-stock body keys unchanged (only the free-text `summary` now carries the size). DeliveryItem.name stays verbatim; adjustKey/adjustItemsWire untouched and match the backend itemAdjust {product_id, variant, name, qty}.",
    "GEOFENCE MATRIX confirmed (absent = old fence, false = none, true = fence). Code: `_fenced => geoExact != false`; `_tooFar` = fix present && fenced && (no pin ? geoExact == null : off > 300 m); `geofenceOk` sent = `_geoReady`. Committed tests cover true+far (blocked), absent+far (blocked), absent+within 300 m (finishes, geofenceOk true), false+4 km (not blocked, geofenceOk true sent - backend C3 `if d.GeoExact && !in.GeofenceOK` ignores it when false), tri-state parse. My throwaway tests proved the two uncovered cells: absent + no pin -> blocked at the geotag step, submit never called (matches release/26.07.03 proof_sheet.dart `_geofenceOk`: `if (g == null || dest == null) return false` plus the deployed backend's unconditional `if !in.GeofenceOK -> GEOFENCE_FAILED`); true + no pin -> finish open, geofenceOk true travels (server measures only when geoSane). Boundary is the same `<= 300 m` as the old `<= 0.3 km`. Net: against the deployed backend nothing loosens or tightens; against C3 the store-fallback case stops blocking. Deploy-order note in the handoff is accurate.",
    "E2E (test/e2e/pack_volume_live_e2e_test.dart) - proven that no assertion can be satisfied by a previously pushed route: (1) flutter_test 3.47.2 binding.dart `_runTestBody` starts EVERY testWidgets with `runApp(Container(key: UniqueKey(), ...))` (line 1941, before the body, unconditional - it runs even after a failed test) and ends a passing one with the same unmount (line 1960), so each of the 11 legs builds a fresh MaterialApp/Navigator; (2) within a leg, `_pumpCard` pushes nothing and `_pumpScreen` pushes exactly one route over `SizedBox.shrink()`, so the only PackSizePill widgets on stage are the surface under test; the route-overview leg taps only the 'Overview' segment (never a stop) and asserts `find.byType(RiderOrderCard) findsNothing`; (3) every module-level slot (`_storeOrder`, `_task`, `_upcomingRows`, `_goldStockRows`) is assigned as the LAST statement of its network leg, after all expects, so a surface leg can only run on data whose leg fully passed, else `_need` fails naming that leg. The Inventory leg is feasible: products_seed.json has gold-500ml and gold-1l both named 'Full Cream Milk - Parag Gold' with variant '500ml'/'1L', so catalogFromStock yields two Gold rows. Still `skip: !kE2E`. I did not boot a backend; the 'each leg fails for its own stated reason against a dead port' claim was not re-verified.",
    "INVENTORY vs adjust flows - no behaviour change on variant-less SKUs. Order-adjust (store_order_screen.dart / adjustItemsWire) never touches StoreSku, catalogFromStock or deriveStock; it is keyed by adjustKey (productId, else name|variant) exactly as before, and the backend adjustKey() falls back to the bare name for old clients. Stock-adjust (catalog_tab.dart in/out-of-stock and price per variant via PATCH /skus/{id}) does not use the inventory model at all. For a variant-less stock row: id = name (same as the old `id => name`), label = name, `matches(name)` and `matches(name, anyVariant)` are true - proven by my throwaway test and by the committed 'a variant-less row takes every variant of its name' test; two variant-less lines of one name still fold; kStoreCatalog (all variant-less) unchanged. `StoreSku.matches` has exactly one caller (deriveStock's canon); `sku.id` is otherwise used only in store_home's low-stock change key.",
    "LOW (documentation/edge, not a compat break) - deriveStock's fallback is wider than its comment says. Proven with throwaway tests: (a) a sized line whose size matches NO row of its name (Gold '5L' with only 500ml/1L rows) lands on the FIRST row of the name (500ml reserved +2), not only 'a line whose size the wire did not carry'; (b) when a variant-less row and a sized row share one name, which row takes a sized line depends on wire order (variant-less first -> it takes the '200g' line via `variant.isEmpty`). The backend emits one stock row per SKU doc with its own variant, so (b) needs a manager-added addition sharing a seeded name with no variant - rare, but worth a sentence in the deriveStock comment or a first-pass exact-variant match before the empty-variant row. Note also the row match is by name+variant TEXT; the backend's `sku_id` / the line's `productId` (gold-500ml) is ignored, so a label drift ('1 L' vs '1L') would fall to the first row of the name.",
    "LOW (stated rule deviation: 'ASCII source (escape glyphs as the series does)') - 7822ce2's three new lib files and ba5fc5d (box-drawing header -> ASCII dashes) set the convention, but 3458ce9 ships lib/screens/store/widgets/store_inventory_row.dart with literal U+20B9 and U+00B7 (lines 28, 31: `'₹$mrp'` and `' ·'` written as glyphs, copied from the removed store_home builder) and new `—` em dashes in three store.dart doc comments; 114a455 adds four box-drawing `─` section headers to the e2e test file (lines 273, 310, 369, 396). No first-person comments found in the series' additions. All new tr() strings have hi.json keys (verified all 13 keys used by the changed files).",
    "LOW (cosmetic, pre-existing map now visible per size) - store_home's `_kMrp` is keyed by product name, so both Gold rows print the same '₹ 33' on their meta line and `soldValue` prices a 1 L sale at the 500 ml MRP; this was already true for the merged row, it just now reads wrong beside a '1L' pill. Also, for absent geoExact + no pin, the error line says 'You are too far from the delivery address' while the geo card says 'Recorded' - reachable only when a task has geo (0,0) (a store without coordinates on the deployed backend).",
    "ALIGNMENT with the recent backend/consumer commits (the user's question): every console-facing backend change since release/26.07.03 has its Saathi counterpart - 9a10786/e6f2045 pack size as `variant` <-> 7822ce2..3458ce9; 731be2e geoExact <-> 792c7a3; 3c02bb1 societyId <-> 51e4936; 083ecf1 GET /stores/{id}/upcoming <-> ef41d7b (404/405/501 -> empty); DROP handover (models.go) <-> 5f19ffb; OTP_UNAVAILABLE/OTP_REQUIRED <-> 4deb0fa/5d86bdc; RING_BELL line (rider_ops_tasks.go `case \"RING_BELL\": return \"Ring the bell and wait...\"` regardless of ring_bell; consumer cart.tsx sends `handover: address.ring_bell ? 'RING_BELL' : 'HAND_TO_CUSTOMER'` with no order-level ring_bell) <-> 5d86bdc; 7897c58 PATCH variants[] was the backend adapting to the shipped catalog tab; 29936df/5b16775 removed then RESTORED the five per-task routes while 0bf6f8b dropped Saathi's dead callers - no wire break either way; 6b374f9 (unsigned proof photos) is the CONSUMER GET /orders list, not /stores/{id}/orders, so no Saathi impact; Kushagra's 0a87418/d6a5f5d/c827ef0 (lanes, pickup point, timing fields) map to Saathi 0a9a4c7/59df595/c5bc08a. I found no frontend read that the committed integration/delivery backend does not serve, and no backend field the consoles need that Saathi does not read."
   ],
   "left_undone": [
    "Did not boot parag-saathi-be or run the live e2e (as instructed); the 'fails per leg against a dead port' claim is the implementer's, not re-verified here.",
    "Backend comparisons were made against the COMMITTED integration/delivery tip, because the backend working tree currently holds unresolved merge conflicts (UU internal/modules/consumer/delivery.go and delivery_svc.go, AA complaints.go) from the other agent; if that merge changes the delivery struct or the deliver handler, the geoExact/geofence comparison should be re-run against the new commit.",
    "No fixes applied (reviewer, edit nothing): the three LOW items - deriveStock comment/first-pass exact-variant match, the ASCII glyphs in store_inventory_row.dart and the e2e headers, and the name-keyed _kMrp beside a size pill - are left for the implementer."
   ]
  }
 ]
}