{
 "merge": "## Merge result\n\n- Merge commit `80f4a0b` on `integration/delivery` (parents `5146fb1` integration, `8394476` release), created with `--no-ff`, no rebase. `git log HEAD..origin/release/26.07.03` is empty; working tree clean.\n- Push: `5146fb1..80f4a0b  integration/delivery -> integration/delivery` (https://github.com/pyaasdairy/parag-saathi-be.git). `origin/integration/delivery` == local HEAD.\n- Repo: `C:\\Users\\jaina\\Downloads\\parag-saathi-be`. Frontend repos were not modified.\n\n## Conflicts and resolution per file (all under `internal/modules/consumer/` unless noted)\n\n| File | Git state | Winner | Why / what was brought across |\n|---|---|---|---|\n| `complaints.go` | add/add | integration base | Keeps `complaint_id` (\"cmp_...\") wire id, `phone`, CRM `complaint.created`/`complaint.resolved` events, admin CRM handlers, `repo.complaints()`. Brought across from owner: pre-insert `FindOne` so a retry is idempotent with the index absent; unknown category -> 400 (blank still files as \"other\"); missing `ref` -> 400; `complaintUpdateInput`, `updateComplaint` (by ref), `listAllComplaints` (unknown status -> 400), `opsListComplaints`/`opsUpdateComplaint`. New: one `answerComplaint` core used by both the Saathi ops route (by ref) and the website admin route (by `complaint_id`), so both emit `complaint.resolved`. Dropped: owner's ObjectID-as-id struct and `complaintsCol()`/`pushCol()`; integration's `newComplaintRef` minting, silent unknown-category default, and the admin handler's inline update. |\n| `push.go` | add/add | integration base | Keeps `DELETE /push/register` (C2), `app_version`, `repo.pushDevices()` that the Expo transport reads. Brought across: strict platform check (ios/android/web; anything else 400, was stored as \"unknown\"), `pushDevicesFor`. Register reply is the union `{\"registered\":true,\"status\":\"registered\",\"delivery\":\"expo\"|\"pending_sender\"}` (delivery reflects `EXPO_PUSH_ENABLED`). Dropped: integration's unused `forgetPushDevices` (erasure cascade already covers it). |\n| `delivery.go` | content | integration | Both added the same five door fields with the same JSON names (`societyId`, `society`, `tower`, `floor`, `unit`); integration block also carries `GeoExact` etc. |\n| `delivery_svc.go` | content | merged | Struct literal: integration (keeps `DeliveryDate`). Door lookup: `address_id` wins outright (`addressByID`, now shared with `addressFor`); without it, the owner's certainty rule replaces integration's first-label-match: candidates = label matches, narrowed to exact `address_text` matches when any (`joinAddress` = the app's composition), door copied only when every candidate is `sameDoor`. Owner's `structuredDoor` type / `composeAddressText` / text-only matching removed; owner's `sameDoor` kept. |\n| `handler.go` | content | integration | Same five `addressInput` fields on both sides; comments only. |\n| `repo.go` | content | integration cascade line; owner index policy | Cascade deletes `consumer_complaints` + `consumer_push_devices` (identical both sides). `ensureIndexes` no longer builds complaint/push indexes: they are built NON-fatally in `module.go` (owner), safe because the pre-insert check makes idempotency index-independent (pinned by `TestPhase2ComplaintIdempotentWithoutTheIndex`). |\n| `service.go` | content | integration | `strings.TrimSpace` on the five fields. |\n| `models.go` | auto-merged, hand-fixed | integration | Auto-merge produced two copies of the five address door fields; owner's duplicate removed. Result byte-identical to integration HEAD. |\n| `module.go` | auto-merged, hand-fixed | both | Auto-merge registered `/complaints` and `/push/register` twice (second bound to nonexistent `h.createComplaint`); owner's duplicate member block removed. Kept: integration member routes (`POST/GET /complaints`, `POST/DELETE /push/register`, `POST /uploads/presign`), owner's non-fatal index build at boot, owner's `cm.Get(\"/ops/complaints\")` + `cm.Patch(\"/ops/complaints/{ref}\")` in the STORE_MANAGER/SUPER_ADMIN group (the 8394476 fix). Three distinct paths (`/complaints` member, `/ops/complaints` Saathi operator, `/admin/crm/complaints` website); no path has two handlers. |\n| `fullchain_e2e_test.go` | auto-merged | both | `ensureSubscriptionDayIndex` (integration) + explicit complaint/push index build (owner). |\n| `phase2_contracts_mongo_test.go` | added | owner (spec) | No assertion changed. Three symbol adaptations: `w.svc.complaintsCol()` -> `w.svc.repo.complaints()` (x3), `w.svc.pushCol()` -> `w.svc.repo.pushDevices()`, `first.ID` -> `first.MongoID` in the `_id` filter; header note added. |\n| `complaints_test.go` | integration test edited | spec | Its \"odd category files as other\" assertion contradicted the spec; now asserts unknown category refused, blank -> \"other\". |\n| `render.yaml` (root) | auto-merged | union | Owner's \"pin is CURRENT\" note + integration's `ADMIN_API_KEY`, `CRM_*`, `EXPO_PUSH_ENABLED`, `EXPO_ACCESS_TOKEN` keys. |\n| `.specstory/statistics.json` | auto-merged | - | one-line tool stat. |\n\nNote: `models.go` is not gofmt-clean on integration already (address struct alignment); untouched, no `gofmt -w` run anywhere.\n\n## Verification\n\n`go build ./...` OK, `go vet ./...` OK. `CONSUMER_MONGO_TEST_URI=mongodb://127.0.0.1:27017 go test ./... -count=1` tail:\n\n```\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/cattle\t0.322s\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/collection\t0.288s\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/consumer\t45.668s\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/identity\t0.208s\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/logistics\t0.201s\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/orgs\t0.226s\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/plant\t0.237s\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/platformops\t0.222s\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/publictrace\t0.570s\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/quality\t0.582s\nok  \tgithub.com/pyaas/saathi-backend/internal/modules/settlement\t0.732s\nok  \tgithub.com/pyaas/saathi-backend/internal/platform/auth\t1.038s\nok  \tgithub.com/pyaas/saathi-backend/internal/platform/dolibarr\t3.043s\nok  \tgithub.com/pyaas/saathi-backend/internal/platform/push\t3.054s\nok  \tgithub.com/pyaas/saathi-backend/internal/platform/sms\t2.912s\n```\n\nConsumer package 45.7 s (> 10 s, Mongo tests ran). `-run 'Phase2|Complaint|Push|AddressID|SocietyID|DoorstepPrefs|Erasure' -v`: all 19 `TestPhase2*` PASS (StructuredAddressRoundTrips, GroundFloorIsNotUnset, TypedAddressStaysUnstructured, DeliveryTaskCarriesTheDoor, AmbiguousAddressYieldsNoDoor, ComplaintIsIdempotentByRef, ComplaintRefsAreScopedPerMember, ComplaintValidation, PushRegisterIsIdempotent, PushTokenFollowsTheCurrentMember, PushValidation, DuplicateRowsSameDoorStillGroup, SameDoorTreatsNilFloorAsDistinct, ErasureTakesComplaintsAndDevices, ComplaintCanBeAnswered, SupportQueue, ComplaintIdempotentWithoutTheIndex, MemberCanReadOwnComplaintsThroughTheRouter, OperatorComplaintPathDoesNotCollide) plus integration's ComplaintFilingIsIdempotentPerMember, PushTokenFollowsTheNewestOwner, PushUnregisterContractC2, ErasureForgetsPushDevices, OrderAddressIDPicksTheDoor, DeliveryTaskCarriesTheSocietyID, DoorstepPrefs* PASS; none skipped.\n\n## Consumer app contract vs merged backend (WT `feature/consumer-revamp-phase2` and `origin/release/26.07.03` = 81a33ec)\n\n| Call | Branches | Served |\n|---|---|---|\n| `POST /complaints {ref, category, order_id?, detail, photo_uri?}` -> app reads `id` | both | yes, 201 with `{id, ref, category, order_id, detail, photo_uri, status, resolution, created_at, updated_at}` |\n| `GET /complaints` -> `[{id, ref, category, order_id, detail, status, resolution, created_at, updated_at}]` | both | yes |\n| `POST /push/register {token, platform: Platform.OS, provider: 'expo'}` (body ignored) | both | yes |\n| `DELETE /push/register {token}` -> 204 | WT only | yes |\n| `POST /uploads/presign {kind, content_type}` -> `{upload_url, method, headers, file_url}` | WT only | yes |\n| `GET /addresses` reads `id, label, line1, line2, city, pincode, is_default, lat, lng, created_at, society, society_id, tower, floor, unit, receiver_name, geo_label, ring_bell, call_before, instructions, door_photo_uri` | both | yes (prefs flattened by `address.MarshalJSON`, models.go:172) |\n| `POST /addresses` sends the same 17 fields; WT reads the full row, release reads `{id}` | both | yes |\n| `PATCH /addresses/{id} {lat, lng}` | release only | yes |\n| `POST /addresses/{id}/default`, `DELETE /addresses/{id}` | both | yes |\n| `POST /orders` with `address_id` (WT) / `address_label` + `address_text` (both) | both | yes: id picks the door; label-only uses the certainty rule |\n\nSpec-vs-app disagreements: none. Spec-vs-integration decisions: unknown category refused (spec); missing ref refused (spec); platform strict but \"web\" kept (integration superset); index policy non-fatal (owner); complaint `id` stays \"cmp_...\" (integration; the app treats it as opaque, CRM events carry `complaint_id`).\n\n## Behaviour changes for the deployed consumer app\n\nVersus the release backend production runs today: complaint `id` is \"cmp_...\" not an ObjectID hex (opaque to the app); push register body differs (ignored); platform \"web\" now accepted; a label-only order (release app sends no `address_id`) gets the door when its label matches one door or identical rows (release matched by composed text only; both refuse ambiguity); `DELETE /push/register`, `POST /uploads/presign`, `/admin/crm/complaints` now exist. Versus integration before the merge: unknown category -> 400 (app sends only the closed enum); missing ref -> 400 (app always sends one); platform outside ios/android/web -> 400 instead of stored \"unknown\"; admin CRM PATCH accepts resolution-only updates and an unknown `?status=` filter is a 400 instead of ignored; complaint/push index failures no longer block boot.\n\n## Frontend alignment findings (pre-existing, outside this me",
 "review": {
  "verdict": "GREEN",
  "build_and_tests": "Repo C:\\Users\\jaina\\Downloads\\parag-saathi-be, HEAD 80f4a0b (merge of 5146fb1 + 8394476), origin/integration/delivery == HEAD, origin/release/26.07.03 (8394476) is an ancestor of HEAD, `git log HEAD..origin/release/26.07.03` empty, no modified tracked files (only the three pre-existing untracked items incl. the secrets zip). `go build ./...` OK, `go vet ./...` OK. `CONSUMER_MONGO_TEST_URI=mongodb://127.0.0.1:27017 go test ./... -count=1`: every package ok; internal/modules/consumer took 66.6 s (>10 s, Mongo tests ran); cattle/collection/identity/logistics/orgs/plant/platformops/publictrace/quality/settlement/auth/dolibarr/push/sms all ok. `-run Phase2 -v`: 19 of 19 TestPhase2* report `--- PASS`, zero `--- SKIP` (StructuredAddressRoundTrips, GroundFloorIsNotUnset, TypedAddressStaysUnstructured, DeliveryTaskCarriesTheDoor, AmbiguousAddressYieldsNoDoor, ComplaintIsIdempotentByRef, ComplaintRefsAreScopedPerMember, ComplaintValidation, PushRegisterIsIdempotent, PushTokenFollowsTheCurrentMember, PushValidation, DuplicateRowsSameDoorStillGroup, SameDoorTreatsNilFloorAsDistinct, ErasureTakesComplaintsAndDevices, ComplaintCanBeAnswered, SupportQueue, ComplaintIdempotentWithoutTheIndex, MemberCanReadOwnComplaintsThroughTheRouter, OperatorComplaintPathDoesNotCollide). The only skip gate is fullchain_e2e_test.go:60-62 on the env var, which was set. gofmt drift (CRLF-normalised) on the 12 merged Go files: 0 lines everywhere except models.go, which has the same 36 lines of pre-existing drift on the integration parent, so the merge introduced none.",
  "findings": [
   "(1) Suite: PASS with Mongo; phase2_contracts_mongo_test.go runs and passes (19/19, none skipped). The spec file differs from the owner's 8394476 version only by the header note and three symbol renames (complaintsCol()->repo.complaints() x3, pushCol()->repo.pushDevices(), first.ID->first.MongoID in the _id filter); no assertion text changed (git diff HEAD^2 HEAD -- phase2_contracts_mongo_test.go).",
   "(2) Consumer app working tree (feature/consumer-revamp-phase2) - every api.get/post/patch/del call in lib and app matched to module.go: /auth/otp/request, /auth/otp/verify (module.go:134-135); /addresses GET/POST, /addresses/{id}/default, DELETE /addresses/{id} (284-288); /orders POST/GET, /orders/{id}, /cancel, /review (292-296); /mandate/me, /mandate/create, /verify, /{id}/pause|resume|cancel|execute (257-263); /catalog (151); /complaints POST/GET (206-207); /users/me/consents GET/POST (229-232); /crm/inbox, /crm/inbox/{id}/read, /crm/offer, /crm/eligibility, /crm/enrol/self (197-201); /me GET/PATCH (219-222); /me/erasure (225); /wishlist/leads, /partner-leads (175-176); /traceability/{code} and getText /traceability/{code}/label (145-146); /push/register POST and DELETE (211-212); /wallet, /wallet/txns, /topup, /order, /verify, /debit, /refund, /promo (235-249); /serviceability, /waitlist (170-171); /subscriptions GET/POST, PATCH /{id}, /{id}/pause|resume|cancel (STATUS_ACTION maps only to those three) (269-276); /trial/me (281); /uploads/presign (216). The two apparent misses, lib/invoice.ts:23 `/orders/{id}/invoice` and lib/quality.ts:91 `/quality/tests`, are comment/TODO lines, not calls. apiClient exposes only get/post/patch/del plus getText; no other verb exists.",
   "(2) Consumer app origin/release/26.07.03 (81a33ec): identical set minus DELETE /push/register and /uploads/presign, plus PATCH /addresses/{id} {lat,lng} from lib/api.ts:269 and lib/location.ts:79 (served at module.go:286) and POST /subscriptions reading {id} (269). All served.",
   "(2) Fields: POST /complaints body {ref, category, order_id, detail, photo_uri} == complaintInput (complaints.go:116-122); app reads id/ref/category/order_id/detail/status/resolution/created_at/updated_at, all JSON tags present on complaint (complaints.go:74-90), id is the 'cmp_' wire id. POST /addresses sends 19 fields (label, line1, line2, city, pincode, is_default, lat, lng, receiver_name, geo_label, ring_bell, call_before, instructions, door_photo_uri, society, society_id, tower, floor, unit) == addressInput handler.go:327-358; reply is the full row via writeJSON 201 (handler.go:419) and address.MarshalJSON flattens the six prefs (models.go:176-195), so the WT's addressFromRemote(created) and the release's {id} both read. floor 0 is sent as 0 and stored via *int (service.go:815-816). POST /orders address_id is read (orders.go:258, 378) and picks the door outright (delivery_svc.go:285-287); label-only orders fall to the certainty rule. Push body {token, platform: Platform.OS, provider} accepted (push.go:69-74); the app ignores the reply and tolerates 4xx.",
   "(3) No duplicates: single collComplaints (complaints.go:48), single collPushDevices (push.go:37), one `type complaint struct` (complaints.go:74), one `type pushDevice struct` (push.go:39), one complaintInput/complaintUpdateInput/pushRegisterInput, one ensureComplaintIndexes (complaints.go:107) and ensurePushIndexes (push.go:59); no complaintsCol(), pushCol(), structuredDoor type, composeAddressText, forgetPushDevices or createComplaint handler anywhere in the non-test package (grep). Door fields appear once each on address (models.go:162-166), addressInput (handler.go:354-358) and delivery (delivery.go:120-124); upcoming.go:50-54 is the separate preview struct. Cascade lists collComplaints and collPushDevices exactly once (repo.go:247). go vet passing rules out duplicate test helpers.",
   "(4) Complaint route registrations, all distinct patterns: member `pr.Post(\"/complaints\")` module.go:206 and `pr.Get(\"/complaints\")` :207 in the consumer-JWT group (svc.authenticate, :192); Saathi operator `cm.Get(\"/ops/complaints\")` :355 and `cm.Patch(\"/ops/complaints/{ref}\")` :356 inside the STORE_MANAGER/SUPER_ADMIN group (:336) under middleware.Authenticate (:328); website admin `ar.Get(\"/crm/complaints\")` admin_crm.go:921 and `ar.Post(\"/crm/complaints/{complaintId}\")` :922 under cr.Route(\"/admin\") -> /consumer/admin/crm/complaints. No pattern is registered twice on the shared /consumer tree; TestPhase2OperatorComplaintPathDoesNotCollide pins the source and TestPhase2MemberCanReadOwnComplaintsThroughTheRouter pins the member read. The Saathi app (both branches) has no caller of /ops/complaints or /push/register yet.",
   "(5) 775f370 hunk-by-hunk: complaints.go const/categories/statuses -> complaints.go:48,54-57,67-69 present; complaint struct -> superseded by :74-90 (MongoID + wire ID); complaintInput -> :116-122; ensureComplaintIndexes (same three indexes) -> :107-114; complaintsCol -> superseded by repo.complaints() :98-100; fileComplaint validation (blank->other, unknown->400, empty detail->400, 4000 cap, ref upper/required, dup-key fallback) -> :128-193; listComplaints -> :196-208; createComplaint handler -> superseded by fileComplaint handler :296-313 (same body); listComplaints handler -> :315-327. delivery.go five task fields -> delivery.go:120-124 (same JSON names). delivery_svc.go strings import -> :11; structuredDoorFor call and literal fields -> :85-87, :95; structuredDoor type/composeAddressText/exactly-one rule -> superseded by *address return, joinAddress (subscriptions.go:931, matches the app's filter(Boolean) join) and the candidate rule :280-320. handler.go addressInput fields -> :354-358. models.go address fields -> :162-166. module.go routes -> :206-207, :211. push.go const/struct/input/indexes -> push.go:37,39-48,69-74,59-67; pushCol -> superseded by repo.pushDevices() :55-57; registerPushDevice (token checks, platform check, provider default, upsert by token with $setOnInsert created_at; token is populated from the equality filter) -> :78-112; pushDevicesFor -> :133-143; reply {status:registered} -> superset :167. repo.go fatal index hunk -> superseded by 640cff7. service.go copies five fields -> service.go:815-816 (trimmed). render.yaml pin note -> render.yaml:13-19. Spec test -> present, all PASS.",
   "(5) 640cff7 hunk-by-hunk: chi import -> complaints.go:11; pre-insert FindOne -> :150-162; complaintUpdateInput -> :214-217; updateComplaint by ref (status validated, resolution capped, empty update refused, 404) -> :264-270 via answerComplaint :224-260; listAllComplaints (status validated, created_at desc, limit 500) -> :274-292; opsListComplaints -> :331-338; opsUpdateComplaint -> :340-352. delivery_svc.go sameDoor rule -> superseded by :280-320 (every candidate must be sameDoor); sameDoor func -> :1319-1331 byte-identical. fullchain_e2e_test.go index build in newChainWorld -> :91-102; the Geo: line whitespace fix -> :278 and file is gofmt-clean. module.go non-fatal index build -> :60-71; cm.Get(\"/complaints\") -> superseded by 8394476. Six new spec tests -> present, PASS. repo.go: complaint/push removed from ensureIndexes (comment at repo.go:129-131, none listed :69-140); cascade adds both collections -> :247.",
   "(5) 8394476 hunk-by-hunk: /ops/complaints and /ops/complaints/{ref} with the collision comment -> module.go:344-356; spec test imports and the two route-guard tests -> present, PASS.",
   "Non-blocking observations (no action required for GREEN): (a) structuredDoorFor returns the chosen row even when its SocietyID is empty, where the owner's version returned nothing; only a partially structured address (tower without society_id) would differ, which the app's picker cannot produce, and the integration parent behaved the same. (b) answerComplaint's 404 text says 'no complaint with that reference' on the admin by-id path too (cosmetic). (c) pushDevice.Token JSON tag is now '-'; nothing serialises pushDevice to the wire, so nothing is lost. (d) Platform set is ios/android/web (owner had ios/android); superset of what Platform.OS sends on the app's targets."
  ],
  "left_undone": [
   "Read-only mandate: nothing was edited, committed or pushed in any of the three repos; no frontend files were changed, so 'aligning the frontend' here means verified-as-served, not modified.",
   "The pyaasdairy.com website repo was not provided, so the website's calls to /consumer/admin/crm/complaints could not be enumerated against admin_crm.go:921-922 (only the backend test crm_lifecycle_test.go:174 covers crmUpdateComplaint).",
   "The Saathi app has no caller for /consumer/ops/complaints on either branch; the operator queue is reachable only by direct API use until a Saathi screen is built. Not a merge defect."
  ]
 }
}