{
 "fix": "All eleven items are committed on `feature/consumer-revamp-phase2` (HEAD 5a48351, 11 commits on top of 7fe34b7, working tree clean, nothing pushed). `tsc --noEmit` exit 0 after every item.\n\n| # | Commit | tsc | What landed |\n|---|---|---|---|\n| 1 | `3d94f95` | clean | `app/subscriptions.tsx`: `SERVER_FREQS = daily/alternate/weekly`; in backend mode the plan editor offers only those (local mode keeps `one_time`); a server plan on any other cadence renders as a read-only pill; `editFreq` refuses a non-server cadence; `freqLabel()` falls back to the raw key. |\n| 2 | `236f4b2` | clean | Generation counters: `addressGen` (`lib/api.ts`), `subsGen` (`lib/subscriptions.ts`), `prefsGen` (`lib/deliveryPrefs.ts`, bumped by every write to the copy via `setCached`). A fetch captures the counter before the await and does not assign when it changed (rows still returned to that caller). Trial cache untouched: it has no invalidation and is written only by its own read, so the race shape does not apply. |\n| 3 | `a87e0f0` | clean | New `clearTrialCache`, `clearDeliveryPrefsCache`, `clearWalletGateSession`; `signOut` calls them plus `invalidateAddressCache`/`invalidateSubscriptionCache` via dynamic import right after `hydratedProfile = null`. `crm.ts unread` left alone (pre-phase-B, and already resets to 0 on the post-sign-out 401). |\n| 4 | `8c6ec89` | clean | `lib/complaints.ts`: `mirrorOutcomeFor(e) === 'drop'` deletes the outbox row; in `fileComplaint` it throws with the server's reason (screen's `submit` already renders `e.message`), in the `refresh` replay loop it sets the store's `error` for that one refresh (`REGISTER_REJECTED`), cleared on the next. Network failures stay queued. |\n| 5 | `4d38f7a` | clean | Outbox row is now `Partial<DeliveryPrefs>` holding only `changedKeys(current, prefs)` (an earlier queued edit rides along); offline reads lay it over cached/defaults. **Deviation, verified in backend:** `service.go updateMe` does `set delivery_prefs = sanitizeDeliveryPrefs(m)` and `sanitizeDeliveryPrefs` builds a fresh doc, so PATCH replaces the whole sub-document; sending only changed keys would have unset the untouched ones. The replay therefore lays the queued keys over a fresh `GET /me` and sends the full object. A true partial PATCH needs a backend merge in `updateMe`. |\n| 6 | `93d7630` | clean | `cancelAutopay` no longer swallows (server re-cancel is idempotent 200, so any error = still live). `deleteMyAccount` stops before `/me/erasure` with an explicit message when a non-cancelled mandate (pending/active/paused) fails to cancel; no live mandate proceeds; phase-B read gate kept. `wallet.tsx`/`autopay.tsx` catch the throw; the profile delete alert now shows the thrown message. |\n| 7 | `226701a` | clean | `walletGate.ts`: `unlocked {uid}` session value proven by balance or `GET /wallet/txns`, only a proven yes remembered, failed read = locked; legacy `pyaas_wallet_unlocked:<uid>` removed on first read. `freePack.ts`: same for `qualified` (`recordRechargeForOffer` sets it); the real key is `pyaas_offer_qualified:<uid>` (the task's `pyaas_free_pack_qualified` does not exist in the repo). Both cleared on sign-out (`clearFreePackSession` added). Readers (`cart.tsx`, `SubscribeSheet.tsx`, `store/wallet.ts`, `recharge.tsx`) unchanged. |\n| 8 | `72653ba` | clean | Backend read: `sweepOneSubscription` places the same-day order only if `wv.Available >= cost`, the delivery debit returns `INSUFFICIENT_FUNDS` to the rider, `crmSweepWalletCover`/B-02 message the member; the pause handler stores only `status` (no reason field). So in backend mode `reconcileWithBalance` now only computes `lowBalance`, writes nothing, and deletes `parag:lowbal:<uid>`; local mode keeps auto-pause/resume. Banner copy in backend mode says a delivery may be skipped rather than \"paused\". |\n| 9 | `6edaf54` | clean | `quality.ts`: backend mode returns `[]` (empty state) and drops `quality_tests` on first read, dev sample rows excluded too. `vip.ts getVip`: backend mode drops the local `vip` row and returns null. |\n| 10 | `8882b55` | clean | `uploads.ts` doc now states the method/headers come from the presign response (POST for Backblaze); `isRemoteUrl` also accepts `/api/v1/uploads/view/...` (`uploads/presign.go ViewPathPrefix`). |\n| 11 | `5a48351` | clean | `HANDOFF-CODEV-2026-09-20.md` gains \"Phase B persistence\": what is local (cart, tokens, session pointer, UI prefs, disclosure records, outboxes with replay handlers, owner-decided device-local items), the reviewer's two consequences, plus the auto-pause and delivery_prefs notes. |\n\nGuards: G1 filter and `backend_id`-on-every-row untouched; G2 unchanged (`TRIAL_UNKNOWN` path intact, `clearTrialCache` only nulls the last answer); G3 strengthened (item 6); G4 untouched (`listAddresses` still error-soft); G5 untouched (`pyaas_setup_done` and the sign-in seed unchanged); G6 untouched (`consentSync` not modified).\n\nLeft undone / notes:\n- Item 5's literal \"PATCH only those\" was not done because the backend replaces the sub-document (evidence above); the outbox-only-changed-keys goal and the no-reset outcome are delivered.\n- `session.ts` `KEEP` regex still spares `parag:vip:*` on sign-out; it is now moot since item 9 drops the row on the next read in backend mode. Left as is (minimal diff).\n- One pre-existing non-ASCII character (`·`) sits on a line item 1 modified in `app/subscriptions.tsx`; it was already in the user-facing copy and was not introduced.",
 "reviews": [
  {
   "verdict": "ISSUES",
   "tsc": "clean: `./node_modules/.bin/tsc --noEmit` exit 0 at HEAD 5a48351 (feature/consumer-revamp-phase2, working tree clean, 11 commits on top of 7fe34b7: 3d94f95 236f4b2 a87e0f0 8c6ec89 4d38f7a 93d7630 226701a 72653ba 6edaf54 8882b55 5a48351; every diff read).",
   "guards": "G1 HOLDS - lib/subscriptionSweep.ts:111-118 filter unchanged (`backendOwned ? !s.backend_id && !['daily','alternate','weekly'].includes(s.frequency) : true`); lib/subscriptions.ts:144-166 subscriptionFromRemote sets `backend_id: sid` (line 161) on every server row; the only rows without one are the create outbox (createSubscription lib/subscriptions.ts:240-253 writes no backend_id), whose recurring cadences the filter excludes. No commit in the range touched either site.\nG2 HOLDS - lib/trial.ts:215-233: backend read failure returns `lastServerTrial` for the same uid else `TRIAL_UNKNOWN` (lib/trial.ts:78, phase 'free', fallback true); the local anchor is never consulted. clearTrialCache (trial.ts:205-207) only nulls the last answer, so a post-sign-out failure falls to TRIAL_UNKNOWN, still free.\nG3 HOLDS (strengthened) - lib/profileApi.ts:72-77 read failure throws before erasure; :82-88 a live (pending/active/paused) mandate is cancelled and a failed cancel throws before /me/erasure (:96-98); lib/walletApi.ts:733-737 cancelAutopay no longer swallows; lib/autopay.ts:164-166 POSTs /mandate/{id}/cancel. Backend confirms: mandate.go:399-401 re-cancel is idempotent 200, mandate.go:59 pending->cancelled is a legal move, and repo.go:237-254 the erasure cascade does NOT touch the mandates collection, so this client-side stop is the only thing revoking a mandate on deletion. Note (pre-existing, outside the range): lib/autopay.ts:117-120 currentMandate returns only the first non-REVOKED mandate and mandate.go:329-350 createMandate has no one-live guard, so a second live mandate would outlive erasure.\nG4 HOLDS - lib/location.ts:70-74 savedAddresses wraps listAddresses().catch(() => []); lib/api.ts:303-308 listAddresses swallows the fetch failure itself and falls to outbox + queued targets; lib/localStore.ts:21-28 getRows never throws; components/SubscribeSheet.tsx:212 createSubscription -> lib/subscriptions.ts:235 hasExactLocation false throws the designed NEEDS_EXACT_LOCATION gate, never a crash. Item 2 did not change the throw surface; item 3's invalidation only forces a refetch.\nG5 HOLDS - lib/session.ts:224-234 seeds hydratedProfile with the server name and writes `pyaas_setup_done:<uid>` BEFORE emit; app/(auth)/otp.tsx:309 passes `pair.profile?.full_name` and id; app/_layout.tsx:153-156 waits for profileLoaded and setupDone before deciding. Untouched by the range. Dependency worth knowing: the pre-existing sign-out purge (lib/session.ts:356-366, since e4a98be) deletes `pyaas_setup_done:<uid>`, so a returning member relies on /auth/otp/verify returning full_name.\nG6 HOLDS - lib/consentSync.ts:226-298 hydrateConsentsFromServer writes the record directly with insertRow (:294), never via recordConsents/queueConsentMirror, carries occurred_at_by_type (:292), and is gated on no pending mirror before (:231) and after (:279) the GET; the mirror handler uses occurred_at_by_type over the synthetic recorded_at (:81-82). No commit in the range touched consentSync.ts or ConsentSheet.tsx.",
   "findings": [
    "[item 4, MEDIUM] A transient failure during token refresh now deletes a complaint and reports it as rejected. Path: lib/apiClient.ts:220-232 on a 401 calls tryRefresh(); lib/apiClient.ts:189-191 doRefresh returns false on a network error/timeout; the refresh token is still present so no sign-out, and :250-255 throws HttpError(401, 'Could not verify your session. Check your connection...'). lib/mirrorQueue.ts:54-59 classifies any 4xx except 408/429 as 'drop', so lib/complaints.ts:185-188 deletes the outbox row and throws 'The register did not accept this complaint... It was not saved', and the replay loop at lib/complaints.ts:258-262 deletes the queued row and shows REGISTER_REJECTED. Before 8c6ec89 this case stayed queued. Concrete: access token expired, member on a flaky link files a complaint -> /complaints 401 -> /auth/refresh times out -> complaint discarded. lib/consentSync.ts:135-139 explicitly treats 401/403 as 'retry' for exactly this reason; complaints should do the same (also 403 from a stale app key).",
    "[item 4, LOW] components/RateAppSheet.tsx:61-71 sendFeedback has try/finally with no catch; fileComplaint now throws on a permanent rejection (lib/complaints.ts:187), so the throw escapes the onPress handler as an unhandled promise rejection: no message, sheet stays open, markRatingSettled never runs. The implementer's 'screen's submit already renders e.message' is true only for app/complaints.tsx:80-81.",
    "[item 3 + item 7, LOW-MEDIUM] The wallet store is an in-memory copy of server state that sign-out does not clear. lib/session.ts:338-348 clears addresses/subs/trial/prefs/walletGate/freePack; lib/auth.tsx:73-85 resets serviceability, notifications and location; nothing resets store/wallet.ts (ZERO at :27-31 is applied only inside refresh() when uid is null, :35-38, and a failed refresh keeps the last-known balance, :67-72). Trace: account A (balance 500) signs out, account B signs in; until B's first successful refresh, components/SubscribeSheet.tsx:188,204 and app/cart.tsx:91 pass A's balance to purchasesUnlocked, and lib/walletGate.ts:47-50 (new in 226701a) now latches `unlocked = { uid: B }` for the whole session off an unproven number. Pre-226701a the stale balance answered true per call only; the ratchet in memory is new. If B's first refresh fails, A's balance is shown as B's for the session.",
    "[item 2, verified correct; residual acknowledged] Interleavings checked against lib/api.ts:270-281/174-177, lib/subscriptions.ts:168-181/136-139, lib/deliveryPrefs.ts:81-86/38-47: (a) GET starts gen=0, mutation lands and bumps gen=1, GET resolves -> not cached, next read refetches; (b) two concurrent GETs both gen=0, mutation, both discarded; (c) addr-create replay POSTs then invalidates after deleteRows, a GET started in between is discarded (wasteful, correct); (d) sign-out bumps gen so a session-1 GET resolving after a same-account sign-in is discarded. Residual: the racing caller still receives and renders the stale rows, e.g. app/delivery-preferences.tsx:19 focus refresh can paint the pre-save value over a toggle the member just saved (lib/deliveryPrefs.ts:84-85 returns prefs it did not cache). Bounded to that one render; next focus corrects.",
    "[item 3, LOW] Late-resolving reads re-populate memory after the sign-out clear (write-after-clear): lib/trial.ts:221 `lastServerTrial = { uid, trial }` runs after clearTrialCache (so the implementer's 'race shape does not apply' to the trial cache is no longer true once a87e0f0 added the clear; consequence bounded to the same uid and only as a failure fallback); lib/walletGate.ts:98 and lib/freePack.ts:226 assign `unlocked`/`qualified = { uid: A }` after account B signed in, overwriting B's proven value so B re-probes the ledger (fail-closed, self-healing). No cross-account leak because every check compares uid.",
    "[item 3, verified] Same-account sign-out/sign-in trace of listAddresses (lib/api.ts:294-315): signOut nulls currentUid (session.ts:331), invalidates the cache and bumps gen (:342), purges every uid-keyed AsyncStorage row in backend mode (:356-366) including parag:addresses, parag:mirror_queue, parag:delivery_prefs, parag:profile_outbox, parag:complaints, parag:user_location; sign-in sets the same uid (:221-222); app/_layout.tsx:197 hydrateAddressCache -> cache miss -> fetchAddresses with the new token -> cached; listAddresses returns the server book merged with an empty outbox and empty addr-default/addr-delete targets. Offline at that moment: server=[] and outbox=[] -> [] (the documented consequence), and the exact-location gate also lost user_location to the purge, so subscribe opens the map.",
    "[item 6, verified] 5xx on POST /mandate/{id}/cancel: lib/autopay.ts:165 throws HttpError(5xx) -> lib/walletApi.ts:735 rethrows -> lib/profileApi.ts:85-87 throws the explicit message -> app/(tabs)/profile.tsx:231 shows it; /me/erasure at profileApi.ts:97 is never reached. Lost-response case: cancel committed server-side but the client saw a timeout -> deletion stops -> retry -> currentMandate finds the mandate REVOKED -> null -> proceeds. Gap: app/(tabs)/wallet.tsx:106 catches the same failure silently (spinner ends, mandate still shown live, no message), while app/autopay.tsx:72 does setErr.",
    "[item 8, verified + LOW] Server pauses while the phone pauses: backend subscriptions.go:598-600 returns the row with 200 when `sub.Status == target`, so POST /pause on an already-paused plan cannot conflict; the phone then invalidates (lib/subscriptions.ts:404) and load() shows PAUSED. Phone-side auto-pause is gone in backend mode (lib/subscriptions.ts:589-593), app/recharge.tsx:177,204 therefore no longer auto-resumes a server-paused plan. Gap: pause on a plan the server has since CANCELLED -> subscriptions.go:601-602 409 SUBSCRIPTION_STATE -> setSubscriptionStatus throws at :403 BEFORE invalidateSubscriptionCache at :404 -> app/subscriptions.tsx:79 setErr only, no load(); that screen's load() (:61,63,67) never passes refresh:true, so the cancelled plan keeps rendering ACTIVE until the home screen's refresh:true read (app/(tabs)/index.tsx:205). A finally-invalidate on the 4xx path closes it.",
    "[item 8, LOW, one-time post-update] The legacy 'sub-status' replay (lib/subscriptions.ts:358-368) can push an old build's queued auto-resume (`row.status === 'active'`) onto a plan the server has since paused: app/_layout.tsx:192 drains the queue before :197 hydration, and fetchSubscriptions:178 deletes the old local rows only after the first fetch. Bounded to a queue that survived the app update; sign-out purges it.",
    "[item 8, LOW copy] In backend mode app/subscriptions.tsx:200-203 still diverts a resume tap on a paused-and-underfunded plan to the sheet, and :336-340 says 'Paused. Your wallet is too low... Add money to resume', implying the phone-side auto-pause 72653ba removed; the server never pauses for low balance (it skips the day) and the resume POST would succeed (paused->active legal at subscriptions.go:64).",
    "[item 9, LOW] lib/vip.ts:83-90 dropTable('vip') deletes exactly the row lib/session.ts:358-364 spares on sign-out because it is 'a PYAAS Plus membership already debited from the SERVER wallet'. purchaseMembership is closed in backend mode (vip.ts:132-134) and isPlusActive is false there (:110), so the row is not honoured either way, but an account that paid on an older build loses its only device-side evidence and there is no GET /membership/me to fall back on. The two sites now contradict each other; one of them should change.",
    "[item 5, verified] Backend replace semantics confirmed: service.go:417 `set delivery_prefs = sanitizeDeliveryPrefs(m)` and models.go:72-97 builds a fresh doc from present keys only, so a partial PATCH resets untouched keys; the replay's GET-then-full-PATCH (lib/deliveryPrefs.ts:157-158) is the right shape. changedKeys/queued-merge (lib/deliveryPrefs.ts:124-127) checked for two offline edits including a revert to the server value: the outbox ends holding the member's last word and the replay lands it.",
    "[item 10, verified] lib/uploads.ts:39 prefix '/api/v1/uploads/view/' matches uploads/presign.go:11 ViewPathPrefix."
   ],
   "left_undone": [
    "Nothing was edited or committed (reviewer role). No runtime/device test was run; every interleaving above is traced on paper against the code at HEAD.",
    "Not verified: whether /auth/otp/verify always returns profile.full_name for a returning member (backend auth module not read); G5 depends on it because the pre-existing sign-out purge removes pyaas_setup_done:<uid>.",
    "Not assessed in depth (pre-existing, outside 7fe34b7..HEAD): the same 401-as-drop classification applies to the addr-create, sub-create and profile mirror handlers; the sign-out purge (lib/session.ts:356-366) discards unreplayed outboxes and the mirror queue, which the item 11 handoff section does not mention; useConsents.save -> recordConsents (components/ConsentSheet.tsx:66-84,138-144) writes a record without occurred_at_by_type so a single toggle re-sends every channel with a fresh occurred_at (user action, not hydration, so G6 as worded is unaffected).",
    "Item 1 (cadence pill) and item 11 (handoff doc) were read and found consistent with the code; no attack surface identified beyond the doc gap noted above."
   ]
  },
  {
   "verdict": "ISSUES",
   "tsc": "./node_modules/.bin/tsc --noEmit at HEAD 5a48351: exit 0, no output (clean). Run by the reviewer, not taken from the report.",
   "guards": "G1 HOLDS: lib/subscriptionSweep.ts:111-118 filter unchanged (backendOwned ? !s.backend_id && !['daily','alternate','weekly'].includes(s.frequency) : true); lib/subscriptions.ts:161 subscriptionFromRemote still sets backend_id = sid on every server row; SubscribeSheet.tsx:31-35 offers only daily/alternate/weekly and item 1 restricts the editor to the same set, so no recurring row can reach the phone-side order path. G2 HOLDS: lib/trial.ts:218-228 unchanged apart from the new clearTrialCache (nulls lastServerTrial only); a failed /trial/me still returns the last same-uid answer else TRIAL_UNKNOWN (phase 'free', fallback true); beginTrial still returns early in backend mode. G3 STRENGTHENED: lib/walletApi.ts:733-737 cancelAutopay no longer swallows; lib/autopay.ts:164-166 cancelMandate has no catch; backend mandate.go:399-401 returns 200 for an already-cancelled mandate (verified), so any throw means still live; lib/profileApi.ts:82-88 stops before POST /me/erasure on a failed cancel of a pending/active/paused mandate, and the read gate at 72-77 is kept. G4 HOLDS: lib/api.ts:294-315 listAddresses swallows a failed fetch (line 307) and returns outbox+queued intents; lib/location.ts:70-74 savedAddresses wraps it in .catch(() => []); createSubscription can only throw the designed NEEDS_EXACT_LOCATION. G5 HOLDS: lib/session.ts:224-234 still writes pyaas_setup_done:<uid> from the sign-in response name before emit; app/_layout.tsx:89-98 gate unchanged; adoptServerProfile (profileApi.ts:172-174) unchanged. G6 HOLDS: lib/consentSync.ts is not in the series diff; hydrateConsentsFromServer still writes occurred_at_by_type (lines 286-294) and the mirror handler still uses timeFor() (81-82), so a hydration record is never echoed as a fresh grant.",
   "findings": [
    "ISSUE (item 4 regression, user-visible): fileComplaint now throws on a permanent 4xx (lib/complaints.ts:185-188), but the backend path of saveSupportTicket (lib/support.ts:164-171) has no catch and its doc comment still says 'never throws', and the caller app/support-chat.tsx:79-83 rate() awaits it with no try/catch after already calling setStage('done'). Scenario: backend mode, the register answers 4xx to POST /complaints -> unhandled promise rejection, the chat is left at stage 'done' with no bot line, no reference and no email escalation offered. Fix is a catch in saveSupportTicket (return null or a ticket with registered:false) or in rate().",
    "ISSUE (item 5 scope, rule deviation): the delivery-prefs outbox is the only outbox not dropped on permanent rejection. (a) saveDeliveryPrefs catch at lib/deliveryPrefs.ts:137-143 queues EVERY error including a 4xx, whereas updateProfile (profileApi.ts:221), addAddress (api.ts:331) and createSubscription (subscriptions.ts:272) throw when mirrorOutcomeFor(e) === 'drop'. (b) the 'delivery-prefs' mirror handler (deliveryPrefs.ts:146-162) throws on a 4xx from GET /me or PATCH /me, so drainMirrorQueue removes the queue op but the delivery_prefs row survives; the profile (profileApi.ts:241), addr-create (api.ts:370) and sub-create (subscriptions.ts:340) handlers all delete their row on 'drop'. Consequence: an orphan row that is laid over every offline read (line 106-108) and merged into every later save (line 124-125, re-sending the rejected keys) until one online save succeeds and drops the table. Low severity in practice (the server clips/accepts the three fields, so a 4xx here is essentially 401/403, which also triggers the sign-out purge), but it is the item's own outbox and the rule the handoff states.",
    "WRITE CLASSIFICATION, backend mode (every insertRow/setRows/putSingle/updateRows/AsyncStorage.setItem/SecureStore.setItemAsync outside lib/localStore.ts, gating verified by reading each function). CART: store/cart.ts (zustand persist, parag_cart_v1). TOKENS: lib/apiClient.ts:119-120. SESSION POINTER: lib/session.ts:221 (parag_current_uid), :202 (login digits for the reviewer gate), :75 (phone-hash->uid index, written at sign-in but only read in local mode), :316 (accounts registry prune on deletion, delete-only). UI PREFERENCE / GATE FLAG: pyaas_setup_done (session.ts:233,418; profileApi.ts:173; complete-profile.tsx:55), WelcomeOffer.tsx:23 seen flag, index.tsx:155 out-of-zone ack, appReview.ts:65/69/77, autoTopup.ts:106/133 (reminder setting + daily nudge stamp), i18n.ts:67 language, orderTracking.ts:169 last-announced status marker, freePack.ts:478/488 snooze/seen, vip.ts:233 upsell snooze, localReset.ts:67 version stamp, freePack.ts:120 claims-purged stamp, store/favorites.ts:39 (owner-decided per the handoff). DISCLOSURE / CONSENT RECORDS (owner-decided): dataConsent.ts:89/106, locationConsent.ts:61, ConsentSheet.tsx:76 (append-only audit trail + mirror enqueue), consentSync.ts:294 (server-hydration record, G6-safe). SANCTIONED OUTBOXES: mirrorQueue.ts:70/103/111 (the queue itself); api.ts:332 addr-create row (deleted api.ts:373 on replay, :370 on drop; :397-398 only mutate an outbox row); subscriptions.ts:273 sub-create row (deleted :343 / :340; :400/:415/:434 only mutate an outbox row); profileApi.ts:223 profile outbox (dropped :217/:241/:244); deliveryPrefs.ts:141 (deleted :136/:160 on success - see ISSUE 2 for the drop gap); complaints.ts:172 (deleted :182/:186/:258/:261); walletApi.ts:522 pending_promos (deleted :544 on replay; NOT dropped on 4xx, but addPromoCredit has zero callers so the row is unreachable today); leads.ts:109 parked restock lead (removed :240 on success or authoritative rejection). LOCAL-MODE ONLY (guarded, unreachable in backend mode): session.ts:238/247/250/441; api.ts:344/353/410/411/604/744/763/788; subscriptions.ts:255/393/496/608; walletApi.ts:179/180/613/614/711/740 (append is reached in backend mode only via ensureBootstrapped inside the dead addPromoCredit); freePack.ts:130 device id/:277/:326; leads.ts:63; referrals.ts:110; support.ts:173; trial.ts:247; quality.ts:103; vip.ts:173/187 (cancelVip is only rendered under vipActive(m), and getVip returns null in backend mode); recharge.tsx calls rechargeWallet only when !creditIsServerSide() (razorpay.ts:95-97 = !isBackendConfigured()). NO VIOLATION FOUND among the 11 items; see the pre-existing grey areas below.",
    "FABRICATED DATA in backend mode: NONE CAN RENDER. Quality: lib/quality.ts:86-94 returns [] and drops quality_tests before any local read; devDemoTests() is only reached in local mode. VIP: lib/vip.ts:83-90 drops the vip row and returns null; isPlusActive() returns false first (line 110); purchaseMembership throws (line 132-134); profile.tsx/vip.tsx/PromoGate.tsx all read through getVip. Seeded wallet rows: DEMO_WALLET_SEED = 0 (session.ts:7) and the seed putSingle is in the local-mode branch only; ensureBootstrapped seeds only from a legacy 'wallet' row with balance > 0 and only via addPromoCredit (no callers) - and in backend mode getBalances/getLedger (walletApi.ts:256, 294) read the server, so even that row could never render. Referrals: listReferrals has no seed and setReferredBy returns false in backend mode. Trial: the local anchor is never read in backend mode.",
    "Item 5 deviation is JUSTIFIED by the backend: service.go:415-417 does set delivery_prefs = sanitizeDeliveryPrefs(m), and models.go:72-119 builds a fresh deliveryPrefsDoc (absent ring_bell -> nil, absent notes -> ''), so PATCHing only the changed keys would reset the others; the replay's fresh GET /me + full PATCH is the correct workaround. Other backend claims verified: subscriptions.go:59/510/628 accept only daily/alternate/weekly (item 1); mandate.go:399-401 re-cancel returns 200, cancelled is terminal at :62 (item 6); subscriptions.go:604-606 pause/resume/cancel $set only status, and :894 sweepOneSubscription requires wv.Available >= cost, INSUFFICIENT_FUNDS at service.go:754 / delivery_svc.go:842 (item 8); uploads/presign.go:11 ViewPathPrefix = /api/v1/uploads/view/ and consumer/uploads_presign.go:86 Method: http.MethodPost (item 10). Item 7: the real key was pyaas_offer_qualified:<uid> as reported; no reader of the two legacy keys remains (grep). Item 3: signOut clears all six in-memory copies before the purge and the emit (session.ts:338-348).",
    "Item 2 race window check: addressGen/subsGen/prefsGen are captured before the await and compared after (api.ts:271/276, subscriptions.ts:169/174, deliveryPrefs.ts:82/84); every invalidation and every setCached bumps them; signOut bumps all three via the clear functions. The trial cache is written only by its own read and keyed by uid, so the implementer's reasoning for leaving it alone holds.",
    "PRE-EXISTING grey areas under the strict rule, not introduced by this series and listed as owner-decided in the item-11 handoff: lib/catalog.ts:445 persists the last GET /catalog response (a read cache of public server data, not member state); lib/userLocation.ts:66 user_location; lib/milk.ts:304 milk_scans; store/favorites.ts:39; lib/notificationCenter.ts:225/237/278/283 local notices (client-generated, deduped against the CRM inbox); lib/subscriptionSweep.ts:127/189 sub_order_markers (phone-side idempotency for phone-placed orders; in backend mode only reachable for a one_time/custom create still in the outbox, which no backend-mode UI can create since product/[id].tsx one_time goes to the cart and SubscribeSheet offers only server cadences); lib/session.ts:84-103 migrateUidData copies legacy u_<digits> rows to the server id once (any resulting backend_id mirrors are deleted on the next fetch; the 2026-08-04.1 local reset already wiped older rows). lib/crm.ts unread (left alone per the report): only stale across an account switch when the first /crm/inbox read after the new sign-in fails transiently.",
    "ASCII: the only non-ASCII byte in the series' added lines is the pre-existing middle dot on app/subscriptions.tsx:92 (a line item 1 re-wrapped), as the report states; no non-ASCII was introduced elsewhere. Commit hygiene: 11 commits, each ends with the required Co-Authored-By line, nothing pushed, working tree clean."
   ],
   "left_undone": [
    "Nothing was edited or committed (reviewer role). The two ISSUES are for the implementer: add a catch in lib/support.ts saveSupportTicket (backend branch) or app/support-chat.tsx rate(); make saveDeliveryPrefs throw on mirrorOutcomeFor(e) === 'drop' and make the 'delivery-prefs' handler dropTable on a 4xx, matching the profile/address/subscription handlers.",
    "Did not run the app or exercise the flows on a device; every classification is from reading the gating code, not from runtime observation.",
    "Did not audit callers of the pre-existing grey-area tables (catalog cache, user_location, milk_scans, favorites, notices, sub_order_markers) for whether the owner still wants them device-local; they are recorded as owner-decided in the item-11 handoff and were out of the 11 items' scope.",
    "Did not verify the backend's promo 403 path (service.go:579 errForbidden 'not available') against config; it only matters if addPromoCredit ever gains a caller, since the pending-promo replay keeps a 4xx-rejected row parked."
   ]
  }
 ]
}