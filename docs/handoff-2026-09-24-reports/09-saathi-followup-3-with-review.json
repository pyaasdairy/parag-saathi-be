{
 "fix": "All five items are committed and pushed; nothing remains that needs another tool call, so this is the report.\n\nREPORT — pyaas-saathi, branch integration/delivery, base e7901be -> head c31bccb (origin in sync: `## integration/delivery...origin/integration/delivery`). analysis_options.yaml left uncommitted throughout (still `M`).\n\nCommits (in order, each followed by flutter analyze clean + full flutter test green, then pushed):\n\n1. 9119b26 fix(rider): the Verify-inventory sheet prints the pack size as its own pill\n   - lib/models/rider.dart: InventoryLine gains optional `variant` (wire `variant`, trimmed, default '') and `size` getter = variant if present else `unit` (where rider_ops_route.go puts \"500 ml\" / \"1 L\" today). No wire key renamed; `name` verbatim.\n   - lib/screens/rider/inventory/inventory_screen.dart: `_lineCard` extracted to public `InventoryLineCard` (line, locked, onTaken, onScan). Meta line is now `Demand N x` + `PackSizePill(size, dense)`; qty and size never share a text run.\n   - test/rider_inventory_test.dart (new, 5 tests): fromWire variant-over-unit; two sizes of a 45-char name at 360 px and 320 px in Roboto -> same name text twice, two pills each inside its card and never cut, `textContaining('2 500')` finds nothing; variant beats unit; verified sheet prints taken count.\n   - Suite: 122 passed, 11 skipped.\n   - Push: e7901be..9119b26 OK.\n\n2. 768ead7 fix(store): deriveStock takes the exact size row first and never lands a sized line on the wrong row\n   - lib/models/store.dart: `StoreSku.sameSize`; `canon` now runs 4 documented passes: (1) exact name+size, (2) variant-less row of the name takes any size, (3) unsized line -> first row of the name, (4) otherwise `null` -> counted in optional `unmatched` map (new named param on deriveStock, keyed `name` or `name|size`, held+delivered only, FAILED ignored). Unmatched units touch no StockRow, so the low-stock banner (`rows.where(low)`) never sees them. Comment rewritten to say exactly which pass takes which line. Callers unchanged (store_home passes nothing).\n   - test/store_inventory_test.dart: +2 tests (exact row wins over a variant-less row listed first; 2 L line on a 500ml/1L catalog + unknown name go to unmatched with both Gold rows untouched and banner filter empty), existing no-size fallback test asserts the bucket stays empty.\n   - Suite: 124 passed, 11 skipped. Push: 9119b26..768ead7 OK.\n\n3. f978b6e chore(store): ASCII source in the inventory row, the three store.dart doc comments and the e2e headers\n   - store_inventory_row.dart lines 28/31 -> `\\u20B9` / `\\u00B7` (verified by od: literal backslash-u bytes, zero non-ASCII in file).\n   - store.dart: inStock, catalogFromStock and deriveStock doc comments -> ASCII dashes; also the two U+2212 minus signs and the \"->\" arrow inside the deriveStock doc (same three comments, noted in the commit message). Other em dashes elsewhere in store.dart untouched.\n   - test/e2e/pack_volume_live_e2e_test.dart: the four `── … ───` headers -> `-- … ---`; file now fully ASCII.\n   - Suite: 124 passed, 11 skipped. Push: 768ead7..f978b6e OK.\n   - Note: the harness rewrites typed `\\uXXXX` in tool input into the glyph (both Edit and Bash heredoc), so the escapes were written via Python `chr(92)`; worth knowing for future ASCII work.\n\n4. 01f3692 fix(store): each pack size prices at its own MRP when the stock line carries one\n   - lib/api/store_console_api.dart: parses optional `price` (else `mrp`) into `StoreStockItem.price` (null for absent/zero/unparseable; the deployed stock wire sends neither — verified in catalog.go storeStockView).\n   - lib/models/store.dart: `StoreStockItem.price`, `StoreSku.mrp`; catalogFromStock carries the first price per name|variant row.\n   - lib/screens/store/store_home.dart: `_mrpFor` -> public `mrpFor` (row's own `sku.mrp` first, else `_kMrp` by name then alias, as before); new `soldValueOf(rows)` used by the Sold today tile. `_kMrp` seed untouched.\n   - test/store_inventory_test.dart: +3 (own MRP per priced size, seed fallback by name and alias; 1 L sale valued at 1 L MRP in soldValueOf; two Inventory rows print their own rupee prices, rupee built with `String.fromCharCode(0x20B9)`).\n   - Suite: 127 passed, 11 skipped. Push: f978b6e..01f3692 OK.\n\n5. c31bccb fix(rider): a task with no delivery pin says so instead of \"too far\"\n   - lib/screens/rider/delivery/deliver_swipe_screen.dart `_captureGeo`: when `_tooFar` and `metres == null` (absent geoExact, no pin) the error is `tr('This task has no delivery pin, ask the store')`; measured-distance case still says too far; fence rule unchanged.\n   - assets/i18n/hi.json: key appended (JSON re-validated, 2688 keys).\n   - test/deliver_swipe_test.dart: `_wire` gains `pin` switch; +1 test (new line shown, \"too far\"/\"Move closer\" absent, card still \"Recorded\", no finish swipe, nothing submitted).\n   - Suite: 128 passed, 11 skipped. Push: 01f3692..c31bccb OK.\n\nFinal analyze/test tail: `No issues found! (ran in 2.7s)` / `00:12 +128 ~11: All tests passed!` (baseline 117 + 11 new tests; the 11 skipped are the opt-in live e2e). The `drag()` hit-test warnings printed during the suite come from the pre-existing test/rider_delivery_ui_test.dart and are unchanged.\n\nLeft undone / observations (not in scope, not touched):\n- Rider inventory \"Products\" metric still counts lines, not distinct names (two sizes of one milk read as 2 products) — same defect the store tile fixed in 3458ce9; one-liner if wanted.\n- ScanProductsScreen subtitle is `line.name` only, so two same-name lines open an identically titled scan screen; could append `line.size`.\n- Backend (parag-saathi-be) read only, not modified; the rider inventory wire's future `variant` key is already consumed by InventoryLine.fromWire.",
 "review": {
  "verdict": "GREEN",
  "tests": "Throwaway probe file C:\\Users\\jaina\\Downloads\\pyaas-saathi\\test\\zz_review_throwaway_test.dart (21 tests, all at 360x800 DPR 1 with Roboto regular+bold loaded from the SDK material_fonts and asserted present) ran green, then was deleted. After deletion: flutter analyze = \"No issues found! (ran in 20.6s)\"; flutter test = \"00:20 +128 ~11: All tests passed!\" (baseline 117 + the implementer's 11; the 11 skipped are the opt-in live e2e). git status afterwards: only \" M analysis_options.yaml\" (the pre-existing tool-made change, not in any commit); HEAD c31bccb == origin/integration/delivery. Probes: ITEM 1 - screen-shaped ListView(padding Insets.l) with two InventoryLineCards of one 45-char name (500 ml short+scannable, 1 L): 2 PackSizePills, 'Demand 2 x'/'Demand 1 x', textContaining('2 500') and ('1 1 L') find nothing, '1 short' and '0/2 scanned' pills render, each pill inside its card and not cut (didExceedMaxLines false), cards within the 360 px gutter, no exception; stepper minus/plus reach onTaken ([0, 2]) and Scan reaches onScan; wire `variant` ' 1L ' wins over unit 'Pouch' (trimmed, Pouch never printed); unit '' -> 'Demand 2' with no pill and no 'x'; wire with no unit -> 'pc' pill; locked card prints the taken count and no stepper; InventorySession.fromWire carries variant and toWire keys are unchanged (product_id, taken_qty, scanned_codes). ITEM 2 - deriveStock: exact size row wins whether the variant-less row is listed first or last, the whole-product row takes the unsized and the 2 L lines, unmatched empty; sameSize is case/whitespace tolerant on both sides; aliases at pass 1, pass 3 and pass 4 (unmatched keyed by the wire name '...PYAAS Gold|2L'); FAILED ignored, DELIVERED counted, null bucket tolerated, rows/soldToday/low untouched; hardcoded catalog: Paneer 200g on the name row, Gold 1L on the PYAAS row via alias, 'Ghee|500g' to the bucket, 8 rows; openingByName still overrides sized rows. StoreInventoryRow at 360 Roboto with the 45-char name, two sizes, own MRPs: pills inside rows and not cut, counts 10/7, both 'Low', exact meta strings 'Pouch U+00B7 U+20B9 33 U+00B7 Sold today 0 U+00B7 Reserved 2' and the 62 twin. ITEM 4 - catalogFromStock keeps the first non-null price per name|size (33, 62, null; openings still summed); mrpFor: own mrp > seed by name > alias > null; soldValueOf = 33 + 2*62 with a priceless Ghee row at zero. ITEM 5 - DeliverSwipeScreen pushed at 360 Roboto: geoExact absent + no pin -> 'This task has no delivery pin, ask the store', 'Recorded', no 'too far'/'Move closer', label 'Swipe to try the geotag again', no finish, two more swipes never submit, both size pills still on the door screen, no exception; absent + pin 4 km -> 'too far' + 'Move closer' and never the pin line; absent + 20 m -> finish opens, geofenceOk true; geoExact true / false + no pin -> no error line, 'Recorded', finish opens (the 792c7a3 rule, unchanged).",
  "findings": [
   "Item 1 proven: InventoryLine gains optional wire `variant` (trimmed) and `size` = variant else unit; InventoryLineCard prints 'Demand N x' + PackSizePill(dense) in a Row(Flexible + pill) so qty and size never share a text run; two same-name sizes are told apart by pills at 360 px Roboto; stepper and scan callbacks still reach the screen (inventory_screen.dart:186-191 wires onTaken via setState). Backend read: rider_ops_route.go riderInventoryLineResponse has product_id/name/demand_qty/unit(omitempty)/scannable/taken_qty and no variant yet; unit comes from the catalogue unit ('500 ml'/'1 kg' per its own comment) else the order line's variant, so the fallback is sound and the future key is already consumed.",
   "Item 2 proven: canon() runs exact name+size (aliases included) -> variant-less row of the name -> unsized line to the first row of the name -> null; null lines are skipped and, when held or delivered, land in the optional `unmatched` map keyed name or name|size, touching no StockRow, so rows.where(low) never sees them. Order of the catalog no longer decides; FAILED orders are ignored. Only callers of matches()/deriveStock() are store_home.dart (Inventory tab and low-stock alert), both pass nothing new.",
   "Item 3 proven for the diff: every ADDED line in e7901be..c31bccb outside hi.json is pure ASCII (grep -P over '+' lines: no hits); store_inventory_row.dart, rider_inventory_test.dart, store_inventory_test.dart and pack_volume_live_e2e_test.dart contain zero non-ASCII bytes; store_inventory_row.dart:28/31 hold literal backslash-u escapes (₹, ·) exactly as 7822ce2 did. Remaining non-ASCII lines in the touched files are pre-existing lines outside the diff (rider.dart 22, deliver_swipe_screen.dart 25, store.dart 23, store_home.dart 40, store_console_api.dart 3, inventory_screen.dart 3, deliver_swipe_test.dart 8), except one noted under left_undone.",
   "Item 4 proven: StoreStockItem.price (parsed from `price` else `mrp`; the deployed storeStockView in catalog.go sends sku_id/name/category/variant/stock_count/in_stock and no price, so today it is always null), StoreSku.mrp carried per name|variant by catalogFromStock (first non-null), mrpFor() prefers the row's own mrp then the `_kMrp` seed by name then alias, soldValueOf() values each row at its own MRP and is what the Sold today tile now uses; `_kMrp` seed untouched. StoreConsoleApi._price is a private static on a singleton over ApiClient.instance (Dio + secure storage) and is not reachable from a test without platform mocks; verified by reading: null -> null, num or num.tryParse, > 0 -> round() else null.",
   "Item 5 proven: _captureGeo now sets the error to tr('This task has no delivery pin, ask the store') only when _tooFar and metres == null (geoExact absent, no `geo`); the measured-distance branch still says too far; _tooFar/_fenced/geofenceMeters (300) untouched; hi.json key appended with a Hindi value, file still valid JSON (2688 keys). geoExact true/false with no pin still open the finish (792c7a3 rule) - unchanged by this series.",
   "No wire key renamed: the lib diff removes no line that reads a wire key (grep of '-' lines for [' or [\" is empty); additions are read-only new keys (`variant` on the rider line, `price`/`mrp` on the stock line); DeliveryItem.name and InventoryLine.toWire are verbatim/unchanged; the only private->public rename is Dart-side (_mrpFor -> mrpFor, _lineCard -> InventoryLineCard).",
   "Scope respected: the 12 changed files are lib/models/rider.dart, lib/models/store.dart, lib/api/store_console_api.dart, lib/screens/rider/{inventory/inventory_screen.dart, delivery/deliver_swipe_screen.dart}, lib/screens/store/{store_home.dart, widgets/store_inventory_row.dart}, assets/i18n/hi.json (one rider key), and four test files (e2e change is four comment headers). models/store.dart is also imported by lib/api/admin_console_api.dart, which uses none of the touched symbols (StoreSku/StockRow/deriveStock/catalogFromStock/DeliveryItem). No onboarding, KYC, collection, pour, plant or other profile file changed; parag-saathi-be untouched. analysis_options.yaml is absent from every commit. All five commit messages end with the required Co-Authored-By trailer.",
   "Minor cosmetic observation (not a defect): when the rider wire sends no unit at all, InventoryLine.unit defaults to 'pc' and the card prints 'Demand 2 x [pc]' - a unit word inside the size pill. Same information as the old 'Demand 2 pc'; backend omits unit only for products the catalogue does not know.",
   "Nit: mrpFor and soldValueOf became public in store_home.dart for the tests without @visibleForTesting (newOrderCount in the same file carries it). Harmless."
  ],
  "left_undone": [
   "lib/screens/store/store_home.dart:315 still carries a U+2014 em dash inside the low-stock banner string ('... .join(', ')} — ${tr('Super Admin alerted.')}'), introduced by series commit 3458ce9 (before the e7901be base) and not covered by the f978b6e ASCII pass; a one-line — change if the owner wants the series fully ASCII. It sits outside the reviewed diff, so it does not block this review.",
   "StoreConsoleApi._price was verified by reading only: the private singleton over ApiClient.instance (Dio with a secure-storage interceptor) cannot be fed a fake wire from a widget test without platform-channel mocks.",
   "Implementer's own out-of-scope notes stand unaddressed: the rider Inventory 'Products' metric counts lines rather than distinct names (inventory_screen.dart:170), and ScanProductsScreen's subtitle is line.name only (inventory_screen.dart:123), so two sizes of one product open identically titled scan screens."
  ]
 }
}